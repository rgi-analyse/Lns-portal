/**
 * Hovedflyt for én analyse-bestilling.
 * Henter én BESTILT, kjører gjennom pipeline, oppdaterer status.
 */
export async function kjørOrchestrator(): Promise<void> {
  console.log('[Orchestrator] Kjører (skjelett — ingen logikk ennå)');
}
